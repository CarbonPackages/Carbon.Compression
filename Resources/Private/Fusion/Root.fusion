error {
    @process.compress = ${Carbon.Compression.compress(value)}
}

prototype(Neos.Neos:Page) {
    httpResponseHead {
        headers {
            'Content-Type' = 'text/html'
        }
    }
}
